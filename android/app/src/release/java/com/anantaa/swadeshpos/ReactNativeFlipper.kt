package com.anantaa.swadeshpos

import android.content.Context

/** Release noop — Flipper is not packaged in production builds. */
object ReactNativeFlipper {
  fun initializeFlipper(@Suppress("UNUSED_PARAMETER") context: Context) {
    // no-op
  }
}
