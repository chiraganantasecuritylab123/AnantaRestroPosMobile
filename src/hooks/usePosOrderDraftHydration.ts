/**
 * Draft orders are stored as a list and restored from the drafts modal.
 * Cart is not auto-hydrated from storage on screen mount.
 */
export function usePosOrderDraftHydration() {
  // Intentionally no-op — multiple draft orders are restored manually.
}
